{% extends 'store/header/base_header.html.twig' %}

{% block header %}
    {{ include('utils/logo.html.twig') }}

    <div>
        <span class="mx-1">{{ include('utils/login.html.twig') }}</span>
        <span class="mx-1">{{ include('utils/register.html.twig') }}</span>
    </div>
{% endblock %}