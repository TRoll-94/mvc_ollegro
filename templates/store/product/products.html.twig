 <div class="d-flex flex-wrap justify-content-center">
    {% for product in products %}
        <div class="m-3">{{ include('store/product/product.html.twig', [product]) }}</div>
    {% endfor %}
 </div>