{% extends 'merchant/base.html.twig' %}

{% block title %}ProductProperty{% endblock %}

{% block container %}
    <div class="">
        <h1>ProductProperty</h1>

        <table class="table">
            <tbody>
            <tr>
                <th>Id</th>
                <td>{{ product_property.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ product_property.name }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ product_property.code }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ product_property.value }}</td>
            </tr>
            <tr>
                <th>Category</th>
                <td>{{ product_property.category }}</td>
            </tr>
            </tbody>
        </table>

        <a href="{{ path('app_product_property_index') }}">back to list</a>

        <a href="{{ path('app_product_property_edit', {'id': product_property.id}) }}">edit</a>

        {{ include('product_property/_delete_form.html.twig') }}
    </div>
{% endblock %}
