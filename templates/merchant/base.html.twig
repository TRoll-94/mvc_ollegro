{% extends 'base.html.twig' %}

{% block title %}Merchant{% endblock %}

{% block body %}
<div class="container mt-5">
  <div class="d-flex align-items-center justify-content-between">
    <div>
      <h1>
        <a class="link-dark link-underline link-underline-opacity-0 link-underline-opacity-75-hover" href="{{ path('app_merchant_index') }}">
          Merchant admin page
        </a>
      </h1>
    </div>
    <div>
      <a class="me-1" href="{{ path('app_store_index') }}">store</a>
      <a href="{{ path('app_logout') }}">logout</a>
    </div>
  </div>
  <p>Hello {{ merchant.name }}</p>
  <hr>
  {% block container %}{% endblock %}
</div>
{% endblock %}
